# -*- coding: utf-8 -*-
"""
AI 프롬프트 템플릿 모듈

🤔 이 파일이 하는 일:
Gemini AI에게 보낼 질문(프롬프트)들을 미리 정의해둡니다.
프롬프트를 한 곳에서 관리하면 나중에 수정하기 쉽고, 일관된 결과를 얻을 수 있습니다.

📚 비유:
- 프롬프트 = 선생님에게 질문하는 질문지 양식
- 이 파일 = 질문지 양식을 모아둔 파일철
"""

from typing import Dict, Any


def get_comprehensive_analysis_prompt(notice_text: str) -> str:
    """
    공지사항 종합 분석을 위한 프롬프트를 생성합니다.

    🎯 목적:
    한 번의 AI 호출로 요약, 날짜, 카테고리, 중요도를 모두 추출합니다.

    🔧 매개변수:
    - notice_text: 분석할 공지사항 텍스트

    📊 반환 형식 (JSON):
    {
        "summary": "200자 이내 요약",
        "dates": {
            "start_date": "YYYY-MM-DD",
            "end_date": "YYYY-MM-DD",
            "deadline": "YYYY-MM-DD"
        },
        "category": "학사|장학|취업|행사|시설|기타",
        "priority": "긴급|중요|일반"
    }
    """
    return f"""
당신은 대학교 공지사항 분석 전문가입니다.
다음 공지사항을 분석하여 아래 JSON 형식으로만 답변해주세요.
다른 설명은 추가하지 말고, 순수 JSON만 출력하세요.

**분석 요구사항:**

1. **summary** (요약):
   - 공지사항의 핵심 내용을 200자 이내로 요약
   - 학생들이 꼭 알아야 할 내용 위주로 작성
   - 불필요한 인사말, 부서명 등은 제외

2. **dates** (날짜 정보):
   - start_date: 행사/일정 시작일 (YYYY-MM-DD 형식, 없으면 null)
   - end_date: 행사/일정 종료일 (YYYY-MM-DD 형식, 없으면 null)
   - deadline: 신청/접수 마감일 (YYYY-MM-DD 형식, 없으면 null)
   - 날짜가 "2월 1일", "2/1" 등으로 표기된 경우, 올해 또는 가장 가까운 미래 날짜로 변환
   - 연도가 없으면 현재 연도 또는 공지사항 맥락상 적절한 연도 추정

3. **category** (카테고리):
   - 다음 중 하나만 선택: 학사, 장학, 취업, 행사, 시설, 기타
   - 학사: 수강신청, 학적, 성적, 졸업, 휴학, 복학 관련
   - 장학: 장학금, 학자금 대출, 등록금 관련
   - 취업: 채용, 인턴십, 취업특강, 진로 관련
   - 행사: 축제, 세미나, 공모전, 대회, 특강 관련
   - 시설: 도서관, 기숙사, 편의시설 관련
   - 기타: 위 카테고리에 해당하지 않는 경우

4. **priority** (중요도):
   - 다음 중 하나만 선택: 긴급, 중요, 일반
   - 긴급: 마감일이 3일 이내이거나, 즉시 조치가 필요한 경우
   - 중요: 대부분의 학생에게 영향을 미치는 필수 공지 (수강신청, 등록금 납부 등)
   - 일반: 특정 학생만 해당되거나 선택 사항인 경우

**JSON 응답 형식:**
```json
{{
    "summary": "요약 내용",
    "dates": {{
        "start_date": "YYYY-MM-DD",
        "end_date": "YYYY-MM-DD",
        "deadline": "YYYY-MM-DD"
    }},
    "category": "카테고리명",
    "priority": "중요도"
}}
```

**분석할 공지사항:**
{notice_text}

**JSON 응답:**
"""


def get_summary_prompt(notice_text: str, max_length: int = 200) -> str:
    """
    공지사항 요약을 위한 프롬프트를 생성합니다.

    🎯 목적:
    긴 공지사항을 짧고 명확하게 요약합니다.

    🔧 매개변수:
    - notice_text: 요약할 공지사항 텍스트
    - max_length: 최대 요약 길이 (기본값: 200자)
    """
    return f"""
다음 공지사항을 {max_length}자 이내로 요약해주세요.

요약 가이드라인:
- 핵심 내용만 간결하게 작성
- 날짜, 장소, 대상 등 중요 정보 포함
- 불필요한 인사말, 부서명 제외
- 학생이 취해야 할 행동 중심으로 작성

공지사항:
{notice_text}

요약 ({max_length}자 이내):
"""


def get_date_extraction_prompt(notice_text: str) -> str:
    """
    날짜 정보 추출을 위한 프롬프트를 생성합니다.

    🎯 목적:
    공지사항에서 모든 날짜 정보를 구조화하여 추출합니다.

    📊 반환 형식 (JSON):
    {
        "start_date": "YYYY-MM-DD",
        "end_date": "YYYY-MM-DD",
        "deadline": "YYYY-MM-DD"
    }
    """
    return f"""
다음 공지사항에서 날짜 정보를 추출하여 JSON 형식으로 답변해주세요.

**추출 규칙:**
- start_date: 행사나 일정이 시작되는 날짜
- end_date: 행사나 일정이 끝나는 날짜
- deadline: 신청, 접수, 제출 마감일
- 날짜가 없으면 null로 표시
- 연도가 없는 경우, 현재 연도 또는 가장 가까운 미래 날짜로 추정
- 모든 날짜는 YYYY-MM-DD 형식으로 변환

**JSON 응답 형식:**
```json
{{
    "start_date": "YYYY-MM-DD",
    "end_date": "YYYY-MM-DD",
    "deadline": "YYYY-MM-DD"
}}
```

**공지사항:**
{notice_text}

**JSON 응답:**
"""


def get_category_prompt(notice_text: str) -> str:
    """
    카테고리 분류를 위한 프롬프트를 생성합니다.

    🎯 목적:
    공지사항을 정확한 카테고리로 분류합니다.

    🏷️ 카테고리:
    - 학사: 수강신청, 학적, 성적, 졸업 관련
    - 장학: 장학금, 학자금 대출 관련
    - 취업: 채용, 인턴십, 취업특강 관련
    - 행사: 축제, 세미나, 공모전 관련
    - 시설: 도서관, 기숙사 관련
    - 기타: 위 카테고리에 해당하지 않는 경우
    """
    return f"""
다음 공지사항을 아래 카테고리 중 하나로 분류해주세요.

**카테고리 목록:**
- 학사: 수강신청, 학적, 성적, 졸업, 휴학, 복학 관련
- 장학: 장학금, 학자금 대출, 등록금 관련
- 취업: 채용, 인턴십, 취업특강, 진로 관련
- 행사: 축제, 세미나, 공모전, 대회, 특강 관련
- 시설: 도서관, 기숙사, 편의시설 관련
- 기타: 위 카테고리에 해당하지 않는 경우

카테고리 이름만 정확히 답해주세요. (예: 학사)

**공지사항:**
{notice_text}

**카테고리:**
"""


def get_priority_prompt(notice_text: str) -> str:
    """
    중요도 판단을 위한 프롬프트를 생성합니다.

    🎯 목적:
    공지사항의 중요도를 3단계로 평가합니다.

    📊 중요도 기준:
    - 긴급: 마감일이 3일 이내, 즉시 조치 필요
    - 중요: 대부분 학생에게 영향, 필수 확인 사항
    - 일반: 특정 학생만 해당, 선택 사항
    """
    return f"""
다음 공지사항의 중요도를 평가해주세요.

**중요도 평가 기준:**
- 긴급: 마감일이 3일 이내이거나 즉시 조치가 필요한 경우
  예) 오늘까지 신청, 긴급 공지, 시스템 장애

- 중요: 대부분의 학생에게 영향을 미치는 필수 공지
  예) 수강신청 일정, 등록금 납부, 졸업 요건

- 일반: 특정 학생만 해당되거나 선택 사항
  예) 동아리 모집, 선택 특강, 시설 이용 안내

중요도만 답해주세요. (긴급, 중요, 일반 중 하나)

**공지사항:**
{notice_text}

**중요도:**
"""


def get_keywords_prompt(notice_text: str, max_keywords: int = 5) -> str:
    """
    핵심 키워드 추출을 위한 프롬프트를 생성합니다.

    🎯 목적:
    공지사항에서 가장 중요한 키워드를 추출합니다.

    🔧 매개변수:
    - notice_text: 키워드를 추출할 텍스트
    - max_keywords: 최대 키워드 개수 (기본값: 5개)
    """
    return f"""
다음 공지사항에서 핵심 키워드를 최대 {max_keywords}개 추출해주세요.

**키워드 추출 가이드:**
- 공지사항의 주제를 대표하는 단어 선택
- 고유명사, 날짜, 장소 등 구체적 정보 우선
- 학생들이 검색할 만한 단어 위주
- 불필요한 조사, 어미 제거

키워드는 쉼표(,)로 구분해서 나열해주세요.
예: 수강신청, 1학기, 2월 1일, 학년별 일정

**공지사항:**
{notice_text}

**핵심 키워드:**
"""


def get_schedule_extraction_prompt(notice_text: str) -> str:
    """
    상세 일정 추출을 위한 프롬프트를 생성합니다.

    🎯 목적:
    공지사항에서 모든 일정을 시간까지 포함하여 추출합니다.

    📊 반환 형식 (JSON):
    {
        "schedules": [
            {
                "event_name": "이벤트명",
                "start_date": "YYYY-MM-DD",
                "start_time": "HH:MM",
                "end_date": "YYYY-MM-DD",
                "end_time": "HH:MM",
                "description": "설명"
            }
        ]
    }
    """
    return f"""
다음 공지사항에서 날짜와 일정 정보를 모두 추출해주세요.

**중요**: 반드시 아래 JSON 형식으로만 답변해주세요. 다른 설명은 추가하지 마세요.

**추출 규칙:**
- 학년별, 대상별로 일정이 다르면 각각 별도의 이벤트로 추출
- 시간이 명시되지 않은 경우 null로 표시
- 종료일/시간이 없으면 시작일/시간과 동일하게 설정
- 날짜는 YYYY-MM-DD, 시간은 HH:MM 형식 사용

**JSON 응답 형식:**
```json
{{
    "schedules": [
        {{
            "event_name": "이벤트 이름",
            "start_date": "YYYY-MM-DD",
            "start_time": "HH:MM",
            "end_date": "YYYY-MM-DD",
            "end_time": "HH:MM",
            "description": "간단한 설명"
        }}
    ]
}}
```

**공지사항:**
{notice_text}

**JSON 응답:**
"""


# 프롬프트 설정 상수
PROMPT_CONFIG = {
    "temperature": {
        "comprehensive": 0.2,  # 종합 분석 (일관성 중요)
        "summary": 0.3,        # 요약 (약간의 창의성)
        "extraction": 0.1,     # 정보 추출 (정확성 최우선)
        "classification": 0.0, # 분류 (완전 일관성)
    },
    "max_tokens": {
        "comprehensive": 2048,
        "summary": 512,
        "extraction": 1024,
        "classification": 256,
    }
}


def get_prompt_config(prompt_type: str) -> Dict[str, Any]:
    """
    프롬프트 타입에 따른 설정을 반환합니다.

    🔧 매개변수:
    - prompt_type: 프롬프트 종류 (comprehensive, summary, extraction, classification)

    📊 반환값:
    {"temperature": float, "max_tokens": int}
    """
    return {
        "temperature": PROMPT_CONFIG["temperature"].get(prompt_type, 0.3),
        "max_tokens": PROMPT_CONFIG["max_tokens"].get(prompt_type, 1024),
    }


# 🧪 테스트 코드
if __name__ == "__main__":
    print("=" * 50)
    print("🧪 프롬프트 템플릿 테스트")
    print("=" * 50)

    # 테스트 공지사항
    test_notice = """
    [학사공지] 2024학년도 1학기 수강신청 안내

    수강신청 일정을 다음과 같이 안내합니다.
    - 4학년: 2024년 2월 1일 10:00 ~ 2월 2일 18:00
    - 3학년: 2024년 2월 2일 10:00 ~ 2월 3일 18:00

    학생지원처 학사운영팀
    """

    print("\n1. 종합 분석 프롬프트:")
    print("-" * 50)
    prompt = get_comprehensive_analysis_prompt(test_notice)
    print(prompt[:300] + "...\n")

    print("\n2. 요약 프롬프트:")
    print("-" * 50)
    prompt = get_summary_prompt(test_notice, 100)
    print(prompt[:200] + "...\n")

    print("\n3. 날짜 추출 프롬프트:")
    print("-" * 50)
    prompt = get_date_extraction_prompt(test_notice)
    print(prompt[:200] + "...\n")

    print("\n4. 프롬프트 설정:")
    print("-" * 50)
    config = get_prompt_config("comprehensive")
    print(f"Temperature: {config['temperature']}")
    print(f"Max Tokens: {config['max_tokens']}")

    print("\n" + "=" * 50)
    print("✅ 프롬프트 템플릿 테스트 완료!")
    print("=" * 50)
